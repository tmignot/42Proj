#ifndef  <%= "#{class_name.upcase}_HPP" %>
#define  <%= "#{class_name.upcase}_HPP" %>

class <%= "#{class_name}" %>
{

	private:

	protected:

	public:
    AUnit(void);
    AUnit(AUnit const & src);
    AUnit           &operator=(AUnit const &);

    virtual         ~AUnit(void);
};

#endif      /* <%="#{class_name}_HPP_" */
